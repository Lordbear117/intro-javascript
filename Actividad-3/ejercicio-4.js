// Numero par o impar con modulo

let numero = '';

numero = prompt("Ingresa un numero");
    
    if(numero % 2 == 0){     
        alert("El numero "+numero+" es par");
    }else{       
        alert("El numero "+numero+" es impar");        
    }